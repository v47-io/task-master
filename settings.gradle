pluginManagement {
    repositories {
        gradlePluginPortal()
        jcenter()
    }
}

rootProject.name = 'task-master'

include 'api', 'test-support', 'impl'

project(':api').name = 'task-master-api'
project(':impl').name = 'task-master-impl'
